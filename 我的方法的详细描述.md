# 基于可信软结构条件的热红外到可见光生成：实验路线

## 1. 研究目标与边界

目标不是声称由单张热红外图唯一恢复真实世界 RGB 颜色、材质或纹理，而是研究：在夜间热红外到日间可见光的无配对生成中，是否能通过**显式、可检查的结构条件**减少目标轮廓断裂、边缘偏移和结构幻觉，并生成结构一致的可见光表观候选。

核心研究问题：

> 相比只在输出图像上施加循环一致性或普通边缘损失，先从热红外图预测软边界及其可靠性，再将可信结构作为生成条件，能否更稳定地保持输入支持的结构？

本方法使用的是数学结构先验与统计学习约束，不是严格热学物理模型；没有辐射传输、温度标定、发射率或传感器响应建模时，不应称为热学物理约束。

---

## 2. 方法定义

给定热红外图像 \(T\)，结构分支 \(S\) 输出：

\[
(E,C)=S(T)
\]

其中：

- \(E\in[0,1]^{H\times W}\)：软边界图，表示“该像素是边界的概率/强度”；
- \(C\in[0,1]^{H\times W}\)：边界预测可靠性，表示“该位置关于边界判断是否可信”；
- \(\bar E=E\odot C\)：可信软边界图。

生成器在第一版中采用 CycleGAN 的热红外到 RGB 方向：

\[
\hat Y=G_{T\rightarrow RGB}(T,E,\bar E)
\]

直观流程：

```text
热红外 T
  ├── 结构预测网络 S ──> 软边界 E、可靠性 C、可信边界 E×C
  └── 原始热红外外观信息
                ↓
       条件融合的 CycleGAN 生成器
                ↓
    结构一致的可见光表观候选 Y_hat
```

注意：\(C\) 不是“边缘概率”。例如道路内部可以是 \(E\approx0,C\approx1\)，表示模型很确定这里不是边界；因此不能只把 \(C\) 单独输入生成器。

---

## 3. 当前数据与可验证范围

当前原始数据为：

```text
/Users/tanwenjie/代码/thermal/FLIR_ADAS_v2/
```

可用的第一版无配对划分：

```text
trainA：images_rgb_train 中 hours == day 的 RGB 图
trainB：images_thermal_train 中 hours == night 的 8-bit TIR 图
testA ：images_rgb_val 中抽取少量 day RGB 图
testB ：images_thermal_val 中 hours == night 的 TIR 图
```

该数据可用于验证“结构条件是否改善无配对 TIR-to-RGB 生成”，但 FLIR v2 的 COCO 标注主要是检测框，不提供全图语义分割边界。因此：

1. 当前阶段可验证普通边缘、弱结构条件、生成边缘一致性和下游检测表现；
2. 当前阶段不能把 C 称为“已校准的全图语义边界可靠性”；
3. 若要严格验证 \(E\) 与 \(C\)，后续需要全图分割标签、目标掩膜，或自建少量精细边界标注验证集。

### 数据预处理规则

所有进入 CycleGAN 与 PearlGAN 比较的图像使用同一份处理结果：

```text
原始图像
  ↓ 强制 resize
500 × 400
  ↓ 中心 crop
360 × 288
  ↓
FLIR_datasets/trainA, trainB, testA, testB
```

训练时再统一执行：随机裁剪 \(256\times256\)、随机水平翻转、归一化到 \([-1,1]\)。不要离线直接裁成 \(256\times256\)。

---

## 4. 从 CycleGAN 开始的原因

CycleGAN 是最干净的无配对生成基线：它没有 PearlGAN 的 TDGA、SGA、MCI 边缘图等已有结构机制。

因此，若在 CycleGAN 上观察到稳定提升，更容易判断提升来自新增的结构条件，而不是原模型的复杂注意力或边缘损失。

代码位置：

```text
/Users/tanwenjie/代码/thermal/pytorch-CycleGAN-and-pix2pix/
```

使用的模型参数应为：

```text
--model cycle_gan
```

不要使用 pix2pix；当前 TIR 与日间 RGB 并非严格一一配对。

---

## 5. 实验组设计：一次只增加一个变量

所有实验必须固定：数据划分、预处理、分辨率、训练轮数、学习率策略、随机种子集合和评价脚本。

### 阶段 A：证明“结构条件”是否有价值

| 编号 | 模型 | 输入条件 | 要回答的问题 |
|---|---|---|---|
| C0 | 原始 CycleGAN | 仅 TIR | 无结构条件的最低基线 |
| C0-cap | CycleGAN + 条件接口 | TIR + 全零条件图 | 排除多输入通道/额外参数带来的提升 |
| C1 | CycleGAN + 硬边缘 | TIR + 二值 Canny/MCI 边缘 | 普通边缘先验是否有帮助 |
| C2 | CycleGAN + 软边缘 | TIR + 连续软边缘 \(E\) | 软结构是否优于硬边缘 |
| C3 | CycleGAN + 可信软边缘 | TIR + \(E\) + \(E\odot C\) | 可靠性是否带来额外价值 |
| C4 | C3 + 打乱 \(C\) | TIR + \(E\) + shuffle\((C)\) | 不是任意图都能带来提升 |
| C5 | C3 + 反转 \(C\) | TIR + \(E\) + \(E\odot(1-C)\) | 可靠性方向是否正确 |

第一版条件融合可采用最小改动：将 \(T,E,\bar E\) 在通道维拼接，经一个小型条件适配层后进入生成器。判别器、CycleGAN 对抗损失和循环一致性损失保持不变。

### 阶段 B：证明不是只超过弱基线

在阶段 A 得到稳定结论后，使用相同图像划分和同一评价协议比较：

| 编号 | 模型 | 目的 |
|---|---|---|
| P0 | 原始 PearlGAN | 已有 TDGA + SGA 的强结构基线 |
| P0-cap | PearlGAN + 空条件接口 | 控制新增接口与参数量 |
| P1 | PearlGAN + \(E\) | 验证软边界在强基线上的作用 |
| P2 | PearlGAN + \(E, E\odot C\) | 验证可信软结构是否仍有增益 |

PearlGAN 的 RGB Canny 与 TIR MCI 边缘图是其原始 SGA 所需数据；若 TIR MCI 被 Canny 替代，实验必须标为工程替代/消融，不能称为严格 PearlGAN 复现。

---

## 6. 结构图 E 与可靠性 C 如何获得

### 6.1 当前 FLIR v2 的可行起步

当前没有全图边界标签时，先不要急于训练一个声称有语义含义的 \(E,C\) 网络。

可以按风险从低到高分三步：

1. **硬边缘 C1**：由 TIR 的 Canny 或 MCI 得到二值边缘，仅验证普通边缘条件；
2. **软边缘 C2**：将边缘距离变换或高斯平滑，得到连续值图。它验证“软条件表示”是否有价值，但不是学习到的语义边界；
3. **可信软边缘 C3**：在获得真实掩膜/边界标注后再正式训练和验证。

### 6.2 具备边界标签后的正式定义

若获得全图分割掩膜或目标掩膜 \(M_{gt}\)，先从掩膜导出边界 \(E_{gt}\)，训练：

\[
L_{edge}=\mathrm{BCE}(E,E_{gt})+\lambda\,\mathrm{Dice}(E,E_{gt})
\]

\(C\) 不能仅由 \(E\) 的 Sigmoid 值代替。可采用两种路线：

- **集成/TTA 路线**：对同一 TIR 做多次增强或多个模型推理；边界预测一致的位置具有较高 \(C\)，分歧大则 \(C\) 低；
- **留出预测正确性路线**：使用模型未参与训练的 out-of-fold 预测，与 \(E_{gt}\) 对比得到正确性标签，再监督 \(C\) 预测“该边界判断是否正确”。

正式可靠性评价应在独立验证集上计算 ECE、Brier Score、错误检测 AUROC/AUPR 和 Risk-Coverage/AURC，且应主要在边界及其邻域内报告，避免大量非边界背景掩盖问题。

---

## 7. 训练损失与评价

### 7.1 CycleGAN 生成损失

阶段 A 保留原始 CycleGAN 的：

```text
对抗损失：生成图像接近目标 RGB 域分布
循环一致性损失：TIR → RGB → TIR 后应保持输入信息
identity loss：可作为原始实现的可选项，但所有组必须一致
```

有标签的正式结构分支加入 \(L_{edge}\) 与可靠性损失；无标签起步阶段不要伪造这两类监督。

### 7.2 指标

| 层面 | 建议指标 | 作用 |
|---|---|---|
| 图像分布 | FID、KID | 生成外观分布是否接近 RGB 域；不能证明单图结构真实 |
| 输入结构保持 | APCE、ECIM | 生成图是否保留输入支持的通用边缘 |
| 下游可用性 | FLIR 检测 mAP | 生成图能否支持目标检测 |
| 有标签边界 | Boundary F1、Boundary IoU、Dice | 结构边界位置是否准确 |
| 可靠性 | ECE、Brier、AUROC/AUPR、AURC | 高 C 是否真的对应低错误 |

不要只依据 FID 选择模型。TIR-to-RGB 是一对多问题，FID 较低不代表生成图严格保留该输入的对象和轮廓。

---

## 8. 判断方法是否有效的标准

不能只展示一两张好看的可视化结果。至少满足以下证据链：

1. C2 优于 C1：软结构优于普通硬边缘；
2. C3 优于 C2：可靠性提供额外增益；
3. C4/C5 退化：打乱或反转可靠性后优势消失，说明模型使用的是有意义的 C；
4. 优势同时体现在结构指标和下游指标，而不仅是 FID；
5. 每组至少使用多个随机种子，报告均值和标准差；
6. P2 在 PearlGAN 上仍有增益，说明方法不只是弥补弱 CycleGAN 缺少结构机制。

如果 C2 有效但 C3 无效，结论应是“软结构条件有效，当前可靠性估计没有提供额外信息”，而不是硬说置信度机制有效。

---

## 9. 实际执行顺序

```text
第 0 步：生成固定的 FLIR v2 数据清单和处理后的 FLIR_datasets
第 1 步：跑通原始 CycleGAN（C0），保存模型、日志和 testB 生成结果
第 2 步：实现空条件接口（C0-cap），确认容量控制公平
第 3 步：加入硬边缘（C1）与软边缘（C2）
第 4 步：固定数据、代码和评价后，检查 C2 是否真的优于 C0/C1
第 5 步：准备真实边界标签或可靠的验证标注，再实现 C 与 C3/C4/C5
第 6 步：复现/运行 PearlGAN（P0），再做 P0-cap/P1/P2
第 7 步：仅在 GAN 结论稳定后，把同一结构条件迁移到条件扩散模型
```

在第 4 步之前，不进入 PearlGAN 改造、可靠性主张或扩散模型；先把最小可验证假设做清楚。

---

## 10. 当前状态记录

- CycleGAN 官方 PyTorch 仓库已在：`/Users/tanwenjie/代码/thermal/pytorch-CycleGAN-and-pix2pix/`；
- PearlGAN 仓库已在：`/Users/tanwenjie/代码/thermal/PearlGAN/`；
- 原始 FLIR ADAS v2 已在：`/Users/tanwenjie/代码/thermal/FLIR_ADAS_v2/`；
- 当前尚未生成统一的 `FLIR_datasets/`、样本 manifest、RGB Canny 边缘或 TIR MCI 边缘；
- 当前尚未验证 CycleGAN 或 PearlGAN 训练可以在本机实际启动；PearlGAN 原代码含 CUDA 专用写法，Mac/MPS 需要单独适配或使用 CUDA 环境。
